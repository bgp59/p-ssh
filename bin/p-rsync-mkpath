#! /usr/bin/env python3

import os
import sys

root_dir = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path = [os.path.join(root_dir, "src")] + sys.path

from p_ssh import p_rsync_mkpath

if __name__ == '__main__':
    sys.exit(p_rsync_mkpath.main())
